{% extends 'base.html.twig' %}

{% block title %}Hello ProductController!{% endblock %}

{% block body %}

  <div class="card-body">
<p class="card-category"> Articles de Plongée</p> 
  {% for article in articles %}
       {{ article.id }}
       {{ article.title }}
       {{ article.category }}
      {{ article.image }}
  {% endfor %}
{# {% for articles in articles %}
<h4 class="card-title ">{{ articles }}</h4>
{% endfor %} #}

</div>
        {# <div class="card only">
        {<h2>
        {# {{ articles.title }}
        -
        {{ article.price }}   
        € #}
        {# </h2>  
            <img src="{{ asset('')}}{{articles.image}}" alt="retest" class="plongee_img"> 
            <div class="card-body"> #}
                {# <h5 class="card-title"> {{article.title}}</h5> #}
                {# <p class="cart-cat"> {{article.category}}</p> #}
                {# <p class="card-text"> {{article.content}}</p> #}
                {# <a href="{{ path('cart_add', {'id': article.id}) }}" class="btn btn-primary">Ajouter au panier</a> #}
                {# <button id="checkout-button">Acheter</button> #}
            {# </div>
        </div>  #}


{% endblock %}

{# {% block javascripts %} (paiement)
  <script type="text/javascript">
      // Create an instance of the Stripe object with your publishable API key
      var stripe = Stripe('pk_test_51IYeeVGYmr5sIrV4tBgGESQ7Kbok4swcFDj8PNwnsjWfIoctPy9d0LQMPNchz5A7TUkcHRFpeKkfkh99gCLwNfIq00RFzlvmyZ');
      var checkoutButton = document.getElementById('checkout-button');

      checkoutButton.addEventListener('click', function() {
        // Create a new Checkout Session using the server-side endpoint you
        // created in step 3.
        fetch('/create-checkout-session', {
          method: 'POST',
        })
        .then(function(response) {
          return response.json();
        })
        .then(function(session) {
          return stripe.redirectToCheckout({ sessionId: session.id });
        })
        .then(function(result) {
          // If redirectToCheckout fails due to a browser or network
          // error, you should display the localized error message to your
          // customer using error.message.
          if (result.error) {
            alert(result.error.message);
          }
        })
        .catch(function(error) {
          console.error('Error:', error);
        });
      });
    </script>
{% endblock %}
  #}